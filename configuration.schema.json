{
  "title": "Solution build configuration",
  "type": "object",
  "properties": {
    "configuration": {
      "description": "Build configuration",
      "enum": [
        "Debug",
        "Release"
      ]
    }
  }
}